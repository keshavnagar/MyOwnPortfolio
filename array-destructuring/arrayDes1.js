// ab apan arrayDes ki help se two number ko bina third varibal ke use kiye swap karnege 

let a = 80 , b = 90;

[a,b] = [b,a];

console.log(`now the a is ${a} and b is ${b} wihout any third variable` );